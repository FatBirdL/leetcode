###题目：请判断一个链表是否为回文链表。

示例 1:

输入: 1->2
输出: false
示例 2:

输入: 1->2->2->1
输出: true
###思路：先创建一个空列表，把链表中的元素搞到列表中去，然后和判断回文字符串的思想相同使用双指针
###代码：
###
def ispalindrome(head):
  temp = []
  while head:
    temp.append(head.val)
    head = head.next
  l = 0
  r = len(temp) - 1
  while l < r:
    if temp[l] = temp[r]:
      return True
    else:
      return False
  return True
###后面基于列表的回文串判断我是可以自己写出来的，但是问题在于，没有借助空列表把链表的元素存到列表中用来遍历
###知识欠缺：head.val,即取出链表中的value/还有就是借助空列表进行while循环的append/还有就是链表可以用.next返回迭代器的下一个项目
###链表是迭代器的啊居然。。。数据结构是时候该看起来了。。。占个坑后面一定补上。。。链表的数据结构
###本题中犯得一个无敌愚蠢的错误！！！居然在对列表元素引用的时候用了（）而非[]？？？？？？aaaaaaaa
